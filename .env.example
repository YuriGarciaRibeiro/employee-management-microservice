# ===========================================
# SENSITIVE VARIABLES - DO NOT COMMIT .env
# ===========================================

# Database Credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=employeedb

# RabbitMQ Credentials
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# JWT Secret (REQUIRED - Must be at least 32 characters)
# IMPORTANT: Generate a strong secret for production!
JWT_SECRET=your-super-secret-key-change-this-in-production-must-be-at-least-32-characters-long

# SMTP/Email Credentials (Optional - leave empty if not using email)
# For Gmail: Enable "App Passwords" in your Google Account
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# PgAdmin Credentials
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
